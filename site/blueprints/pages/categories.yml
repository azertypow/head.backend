title: List of Categories

fields:
  categories:
    sortBy: name asc
    label: Categories
    type: structure
    fields:
      name:
        options:
          update: false
        label: Name
        width: 1/3
        type: text
      slug:
        label: Slug
        width: 1/3
        type: slug
        wizard:
          field: name
          text: Generate!
        help: '
            Must be a unique ID, to link tag to site content, and change the Name value if necessary.
            (Slug is forthe link, name, for the text to show)
          '
